initSidebarItems({"struct":[["hash","A argon2i hash of the user's password"],["salt","Salt used to generate the password hash"],["star","Represents `table_name.*`, which is sometimes needed for efficient count queries. It cannot be used in place of `all_columns`, and has a `SqlType` of `()` to prevent it being used that way"],["username","Username for the user. Also the primary key"]]});